apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - HPA.yaml

# Optional: Add labels, prefix, or configmap/secret generators
labels:
  - includeSelectors: true
    pairs:
      team: devops
      app: frontend